import { useState } from "react";
import { ArrowRight, Mail, MapPin, Phone } from "lucide-react";
import { sendInquiry } from "../api/client";
import { Button } from "../components/ui/Button";
import { Input } from "../components/ui/Input";

export function Contact() { const [form, setForm] = useState({ name: "", email: "", message: "" }); const [status, setStatus] = useState(""); const submit = async (e) => { e.preventDefault(); setStatus("sending"); try { await sendInquiry(form); setStatus("sent"); setForm({ name: "", email: "", message: "" }); } catch { setStatus("error"); } }; return <div><section className="contact-layout"><div><p className="eyebrow">COME SAY HELLO</p><h1>Let’s make<br /><em>something strong.</em></h1><p className="contact-intro">Have a question, want to tour the space, or ready to start? Drop us a line and a real human will get back to you.</p><div className="contact-details"><span><Mail size={18} />hello@ironhouse.fit</span><span><Phone size={18} />020 7946 0958</span><span><MapPin size={18} />18 Foundry Lane, London E8</span></div></div><form className="contact-form" onSubmit={submit}><Input label="Your name" required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} placeholder="Alex Morgan" /><Input label="Email address" required type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} placeholder="alex@example.com" /><label className="field"><span>How can we help?</span><textarea required minLength="10" value={form.message} onChange={(e) => setForm({ ...form, message: e.target.value })} placeholder="I’d like to come in for a tour..." /></label><Button type="submit" disabled={status === "sending"}>{status === "sending" ? "Sending..." : <>Send inquiry <ArrowRight size={16} /></>}</Button>{status === "sent" && <p className="success-text">Thanks — we’ll be in touch shortly.</p>}{status === "error" && <p className="error-text">Something went wrong. Please try again.</p>}</form></section></div>; }
